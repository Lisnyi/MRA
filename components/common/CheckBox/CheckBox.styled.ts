import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    border: {
        display: 'flex',
        width: 20,
        height: 20,
        borderWidth: 1,
        borderRadius: 50,
        alignItems: 'center',
        justifyContent: 'center',
    },
    active: {
        width: 14,
        height: 14,
        borderRadius: 50,
    }
})