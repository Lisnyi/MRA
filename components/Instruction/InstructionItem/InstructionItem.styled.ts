import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    box: {
        borderWidth: 1,
        marginBottom: 10,
        padding: 2,
        borderRadius: 4,
        alignItems: 'center',
    },
    image: {
        width: '100%',
        marginBottom: 10
    },
    description: {
        textAlign: 'center',
        fontFamily: 'NotoSans-Regular',
        fontSize: 16,
        lineHeight: 20
    }
})