import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    box: {
        display: "flex",
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center'
    },
    element: {
        width: 20,
        height: 20,
        marginRight: 2,
        borderWidth: 1,
    }
})