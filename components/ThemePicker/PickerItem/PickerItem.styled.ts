import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    box: {
        display: 'flex',
        width: 150,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'flex-start'
    }
})